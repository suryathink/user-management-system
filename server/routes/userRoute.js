const { Router } = require('express');

const UserSchema = require('../model/userSchema');

const router = Router();

router.get('/',async (req, res)=>{
    try {
        
    } catch (error) {
        
    }
})

